#include <boost/regex.hpp>
#include <iostream>
#include <string>
#include <

using std::string;
using std::cin;
using std::cout;

int main(int argc, char *argv[]) {
    string line;

    if (argc == 1) {
        // enter interactive mode
    } else if (argc >= 2) {
        // execute the script
    }
}
